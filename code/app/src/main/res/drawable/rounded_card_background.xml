<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <corners android:radius="16dp" /> <!-- 둥근 모서리 -->
    <solid android:color="@android:color/white" /> <!-- 배경 색상 -->
    <stroke
        android:width="1dp"
        android:color="@android:color/darker_gray" /> <!-- 테두리 -->
    <padding
        android:left="16dp"
        android:right="16dp"
        android:top="12dp"
        android:bottom="12dp" />
</shape>
